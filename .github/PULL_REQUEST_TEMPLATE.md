<!-- Keep it short. Delete sections that don't apply. -->

## What & why

<!-- What does this change do, and why is it needed? -->

## How it works

<!-- Key mechanism or approach a reviewer should understand. Skip if obvious from the diff. -->

## Checks

- [ ] `npm run typecheck` passes (bot + dashboard)
- [ ] `npm run lint` (oxlint) and `npm run lint:ast` (ast-grep) are clean
- [ ] No secrets, session files, or local `data/` contents committed
